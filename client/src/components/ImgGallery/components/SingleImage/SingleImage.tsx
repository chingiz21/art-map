import { observer } from 'mobx-react-lite';
import React from 'react'
import './SingleImage.css';

const SingleImage = observer(({ imgState }: any) => {


    return (
        <div>single image</div>
    )
}
)

export default SingleImage